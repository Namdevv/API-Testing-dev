services:
  agent:
    command: ["uvicorn", "main:app", "--reload", "--host", "0.0.0.0"]
    ports:
      - "8080:8000"
    volumes:
      - ${PROJECT_ROOT}/:/app
    environment:
      - DEBUG=True
    # profiles:
    #   - disabled

  database:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"